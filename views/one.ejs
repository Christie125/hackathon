<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Room One</title>
    <link rel="stylesheet" href="css/duck.css">
</head>
<body>
    <p id="typewriter" class="type"></p>
    <script>
        document.addEventListener("DOMContentLoaded", function() {
            const text = "You come across a duck in the forest near when you find the letter.";
            let i = 0;
            function typeWriter() {
                if (i < text.length) {
                    document.getElementById("typewriter").innerHTML += text.charAt(i);
                    i++;
                    setTimeout(typeWriter, 50);
                }
            }
            typeWriter();
        });
    </script>
    <div class="popup" id="popup">
        <p> I'm buried in the dirt, not a seed in sight, </p>
        <p> I grow underground, hidden from the light. </p> 
        <p> Mashed, fried, or baked, I'm quite the delight, </p> 
        <p> What am I, round and brown, a tasty bite? </p> 
        <form action="/onesolve" method="POST">
            <input type="text" name="code" id="code">
            <button type="submit">Submit</button>
        </form>
    </div>
    <div class="duck" onclick="openPopup()"><img src="/images/duck.png" alt="Duck"></div>
    <script src="popup.js"></script>
</body>
</html>